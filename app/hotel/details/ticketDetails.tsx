import { View, Text } from 'react-native'
import React from 'react'
import TicketDetailsScreenComponent from '@/components/screen/tickets/TicketDetailsScreenComponent'

type Props = {}

const ticketDetails = (props: Props) => {
  return (
   <TicketDetailsScreenComponent/>
  )
}

export default ticketDetails